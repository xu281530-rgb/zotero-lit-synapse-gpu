\# 上一轮完成内容：
全部完成。类型检查、ESLint、55 个测试脚本、构建均通过。



\---

修改报告



先说三个真实缺陷（不是文案问题，是数据残留）



核对源码后发现销毁路径完全没有覆盖关键词索引，而构建路径一直是覆盖的——这个不对称就是你说的"向量已删除、旧关键词索引残留"的根因：



┌────────────────────────────────────────────────────────┬───────────────────────────────────────────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────┐

│                          路径                          │                        原行为                         │                                               后果                                               │

├────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────┤

│ 删除全部（clearAll）                                   │ 只清空 6 张向量表，kw\_terms/kw\_docs/kw\_postings       │ 点完"删除全部"后，整个关键词索引仍在，keyword\_search 继续返回结果                                │

│                                                        │ 原样保留                                              │                                                                                                  │

├────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────┤

│ 全库重建（clearLibraryForBuild）                       │ 只删向量，然后提交 reset\_completed=1                  │ 更糟：断点续建会认为"已清理过"，那批旧倒排永远不会被清                                           │

├────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────┤

│ 删除单篇（hooks.ts 4 处直连                            │ 只删向量                                              │ 条目被永久删除、附件被删、右键"清除选中条目索引"、右键"清除该分类索引"——四条路径全部留下倒排记录 │

│ vectorStore.deleteItemVectors）                        │                                                       │                                                                                                  │

└────────────────────────────────────────────────────────┴───────────────────────────────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────┘



修法



\- clearAll 的白名单从 SEMANTIC\_BUSINESS\_TABLES 换成 INDEX\_BUSINESS\_TABLES（新增 KEYWORD\_BUSINESS\_TABLES），前后行数统计、"必须清空"断言、sqlite\_sequence 重置一并覆盖两套。

\- clearLibraryForBuild 在同一个事务里、在提交 reset 标志之前调用新的 clearWithoutTransaction(libraryID)。顺序有断言钉住。

\- 关键词删除移进 deleteItemVectors / deleteItemsVectors 这个咽喉点，而不是留给每个调用方。deleteItemIndex 里那次重复调用随之删掉。这样将来第五个调用方也不可能漏。

\- 顺带把 clear(libraryID) 从"每个词项一条 DELETE"改成一条子查询 DELETE（term\_id IN (SELECT … WHERE library\_id=?)）。kw\_terms 上有 UNIQUE(library\_id, term)，所以这是等价改写，不是近似。



未修：你机器上已经存在的残留不自动清理（按你的选择）。点一次「重建」或「删除全部」即可清掉。



\---

1\. 设置面板



\- pref-embedding-title：「语义搜索」→「搜索」（6 个语言），描述同步改成"配置检索所用的 Embedding API，并管理语义向量索引与正文关键词索引"。

\- 「自动更新索引」开关从下方「混合检索」面板搬回索引操作按钮正下方（构建/重建/重试失败/删除全部 之后、消息区之前）。

\- 顶部四格改为合计：



┌──────────────────┬────────────────────────────────────────────────┐

│        格        │                       值                       │

├──────────────────┼────────────────────────────────────────────────┤

│ 文献（合计）     │ max(向量索引文献数, 关键词索引文献数)          │

├──────────────────┼────────────────────────────────────────────────┤

│ 索引记录（合计） │ 向量数 + 倒排记录数                            │

├──────────────────┼────────────────────────────────────────────────┤

│ 数据库           │ 文件总大小（本来就含两套，按你的选择不再分列） │

├──────────────────┼────────────────────────────────────────────────┤

│ 状态             │ 不变                                           │

└──────────────────┴────────────────────────────────────────────────┘



「文献」用 max 而不是相加，因为一篇文献可能同时在两套索引里——这是并集不是和。当一套是另一套的子集时（共享构建流程产生的正常状态）max 就等于并集；绝不会高估，只会在两套正在重新同步时短暂低估。这条推理写在代码注释里。



2\. 详细统计



按你要求分成连续两个区域，语义索引区含 API 用量（原有内容一项没删）：



语义索引：已索引文献、向量数量、中文向量、英文向量、缓存、缓存大小、维度、Int8 优化 → 然后是原来的 API 累计（Token / 请求 / 费用 / 限速触发）。



关键词索引（新增，纯数值，无说明文字）：

\- 已索引文献 · 正文 Chunk 数 · 词项数 · 倒排记录数 · 已索引正文 · 无正文



"成功/失败"按你的选择换成\*\*「已索引正文 / 无正文」\*\*——现有数据模型里关键词写入失败只打日志、库里查不出来，写"失败数"就是编数字。这条决定写在 KeywordIndexReport 的文档注释里，并有断言保证两者之和恒等于文献总数（不存在第三个桶）。



\- 刷新统计：refreshAllStats 一个入口同时刷新两套 + API 用量，不可能只刷一半。

\- 重置统计：语义保持原义——只清零 API 用量计数器。两套索引的每个数字都是真实行数的直接计数，没有可"重置"的计数器，在这里删索引是伪装成统计重置的数据丢失。改动只有两点：确认文案明确写出"不会删除语义索引或关键词索引"，以及重置后重绘两个索引区让面板一致。



3\. 文案清理



\- 删掉 get\_item\_fulltext：它在 6 个语言的 MCP 配置指南里活了很久（config-guide-tools-list）。整张工具清单重写，补上 get\_document\_chunks，hybrid\_search 从"不扫描全文"改成"关键词（元数据 + 已索引正文）与语义两路并行"，search\_fulltext 从"二阶段全文搜索"改成"在单篇文献内部检索"。

\- find\_similar 分页：源码实际是 resolveResultCap(args.topK, settings.maxDocuments)——每页数量就是用户设置的最大返回文献数，"20"只是默认值。工具描述、topK schema、两份 README 全部改成"每页返回数量受用户设置的最大返回文献数限制"。纯语义定义未动：没有加任何关键词参数或评分，测试断言 find\_similar 的 schema 里不得出现任何 keyword 字样。

\- search\_fulltext 的 minKeywordScore：仍写着"on the normalised BM25F scale"——错的。chunk 级用的是 rankLexicalCandidates（IDF 在该文献自己的 chunk 上算），不是 BM25F。描述正文早就写对了，参数说明在自相矛盾。已改。

\- "语义索引"作为两套索引总称的地方改为「搜索索引 / 索引」：右键菜单的清除确认与索引开始/失败提示、MinerU 统一流程说明、翻译说明、Chunk 重建警告与过期横幅、删除全部/重建的确认文案。只在专指向量部分时保留"语义索引"（如详细统计的分区标题、find\_similar 说明）。6 个语言全覆盖，构建无新增缺 key 警告。



\---

4\. 新增/修改的测试



scripts/test-index-sync.js（新，17 个用例，npm run test:index-sync）——真 SQLite，不是 mock，因为出问题的是 SQL 不是控制流：



\- 按库清理：另一个库的倒排、词项完好；无孤儿 posting（term\_id NOT IN kw\_terms 必须为 0）

\- clearWithoutTransaction 与 clear 结果逐位相同

\- 重写 + compact 后无旧版本残留（tombstone 与其 posting 都被回收）

\- 统计口径逐项核对（含"有正文 + 无正文 = 总数"）、空索引返回全 0、删除单篇后统计跟着变

\- 接线断言：deleteItemVectors/deleteItemsVectors 必须调 removeKeywordIndex；clearIndex 必须两路都清；clearLibraryForBuild 必须用无事务版本且在 reset 标志之前；构建 finally 里必须 compact

\- 文案断言：find\_similar 不得写死页大小、不得有关键词参数；任何工具不得再声称"不搜索正文"；search\_fulltext 的关键词阈值不得声称 BM25F；6 个语言的配置指南只能出现真实存在的工具名



scripts/test-index-lifecycle.js（改）：重建事务里的 DELETE 从 4 条变 7 条，新增断言要求 kw\_postings → kw\_docs → kw\_terms 按序删除、只删目标库、且全部早于 reset 标志；clearAll 白名单断言扩展到两套表，并逐张点名（防止切片对齐掩盖删除）。



反向验证（确认测试真的会失败，不是摆设）：

\- 抽掉 clearWithoutTransaction 调用 → 16/17，重建用例失败

\- 抽掉 removeKeywordIndex + 清空关键词表白名单 → 15/17 + test-index-lifecycle 断言失败



\---

验证结果



npx tsc --noEmit    → 0

npx eslint .        → 0

55 个测试脚本        → 55 pass / 0 fail

npm run build       → ✔ Build finished in 0.394 s

构建期本地化警告 42 条，全部是既有的 pref-translation-\* / pref-mineru-unified-flow 在 de/es/fr/ja 缺失，本轮新增的 key 一条都不缺。



\---

发现但未修改



1\. 已有残留不自动清理 —— 你选的。手动点一次「重建」或「删除全部」即可。

2\. 倒排记录数 是全表计数，不按库拆分。 posting 只存 slot，要按库过滤得回连 kw\_docs 做区间判断，代价远高于这个数字的价值，而面板每 5 秒刷新一次。单库用户（常态）这个数就是精确的；已在类型注释里写明。

3\. 关键词索引仍无失败记录。 想要真正的"成功/失败"必须给 kw\_docs 加字段并做一次轻量库升级——你已选择改用「已索引正文 / 无正文」，所以没动数据模型。

4\. preferences.ftl 里有一批孤儿 key（pref-semantic-stats-total-items、-db-size、-status、-progress、-refresh-button），改版后已无人引用。Fluent 容忍未使用的 key，删它们属于无关清理，没做。

5\. search\_fulltext 仍接受两个未写进 schema 的旧别名：单元素 itemKeys 和 q。是有意的向后兼容，保留。

6\. pref-translation-\* 在 de/es/fr/ja 全缺（42 条警告），本轮之前就存在，与本次改动无关，未动。



\# 本轮正在进行的内容：

继续检查并修改当前 Zotero LitSynapse 插件源码。上一轮主体修改已经完成，本轮只处理以下遗留问题。\*\*不要根据上一轮报告直接假定问题存在或修法正确，先用源码、真实 SQLite 查询和测试验证，再修改。不要顺带重构无关代码。\*\*



\### 1. 修正顶部“文献（合计）”统计



当前实现使用：



`max(语义索引文献数, 关键词索引文献数)`



这并不是真正的并集。例如语义索引有 A、B，关键词索引有 B、C，真实合计应为 3，而不是 2。



请改为按当前 library 的真实文献身份（itemID/itemKey，以现有数据模型实际可靠主键为准）计算：



\*\*语义索引文献 ∪ 关键词索引文献\*\*



要求：



\* 不重复计数。

\* 必须按当前 library 隔离。

\* 正常同步、部分索引、异常残留状态都必须准确。

\* 增加交叉集合测试，禁止重新使用 `max()` 近似。



\### 2. 修正关键词“倒排记录数”的统计口径



当前报告称倒排记录数直接统计整张 postings 表，因此多 library 时会混入其他库。



请改为：



\*\*只统计当前 library 实际拥有的倒排记录。\*\*



根据真实表结构选择 JOIN、EXISTS、子查询或其他正确方式，不要因为刷新频率而接受错误统计。



要求测试至少覆盖：



\* 两个 library 同时存在；

\* 当前库统计不会包含另一个库；

\* 删除一个库/单篇条目后统计立即正确变化。



如果查询性能存在问题，请实际 benchmark 后优化 SQL，而不是降低统计准确性。



\### 3. 补全两套索引各自的数据库占用



我要求的详细统计是：



\*\*语义索引\*\*



\* 保留原有详细统计和 API 用量；

\* 增加/保留“数据库占用”。



\*\*关键词索引\*\*



\* 必须单独显示“数据库占用”。



顶部“数据库”仍显示整个搜索索引数据库的总体占用。



请先检查实际 SQLite 存储结构，确认能否可靠拆分两套索引占用。\*\*禁止用整库文件大小重复冒充两套各自占用，也禁止编造数值。\*\*



优先寻找能够真实统计相关表/索引占用的 SQLite 能力；如果运行环境确实无法得到可靠的物理占用，请给出证据，并实现最准确、定义清晰且不会误导用户的统计口径，同时在最终报告说明口径。UI 中仍只显示“统计项 + 数值”，不要增加解释性长文。



\### 4. 同步检查



完成后重新检查：



\* 刷新统计一次刷新语义、关键词和顶部合计；

\* 重建、删除、单篇删除后统计立即一致；

\* 多 library 下所有统计严格隔离；

\* 不破坏上一轮已经完成的索引生命周期同步和 `find\_similar` 纯语义行为。



最后运行 TypeScript 类型检查、ESLint、全部测试和构建。新增针对以上三个问题的回归测试，并进行反向验证，证明测试在恢复旧实现时确实会失败。



最终只报告：



1\. 实际确认的问题；

2\. 修改内容；

3\. 新统计口径；

4\. 测试与构建结果；

5\. 仍未解决的问题。



claude最后进度：

Now the preferences pane:

&#x20; Ran 1 shell command

Now add the two per-index storage rows. First the element lookups in preferenceScript.ts:

&#x20; Searched for 1 pattern, ran 2 shell commands

Now the XHTML rows and locale strings:

&#x20; Ran 5 shell commands

Now let's typecheck before writing tests:

&#x20; Searched for 4 patterns, read 1 file, listed 1 directory, ran 6 shell commands

● Write(scripts\\test-index-statistics.js)

Wrote 540 lines to scripts\\test-index-statistics.js



